const <%= serviceName %>Routes = require('./<%= serviceName %>.routes');
const <%= serviceName %>Services = require('./<%= serviceName %>.services');

module.exports = { <%= serviceName %>Routes,  <%= serviceName %>Services };
