import <%= serviceName %>Routes from './<%= serviceName %>.routes';
import <%= serviceName %>Services from './<%= serviceName %>.services';

export default  {<%= serviceName %>Routes , <%= serviceName %>Services}